spring:
  profiles:
    active: test

#jdbcTemplate sql log
logging:
  level:
    org:
      springframework:
        jdbc: debug
    hello:
      itemservice:
        repository:
          mybatis: trace

#MyBatis
mybatis:
  type-aliases-package: hello.itemservice.domain
  configuration:
    map-underscore-to-camel-case: true

